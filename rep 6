package main

import "fmt"

func main() {
	var num int
	println("escreva um numero")
	fmt.Scan(&num)
	for i := 1; i <= 10; i++ {
		resultado := num * 1
		fmt.Printf("%d x %d=  %d\n", num, i, resultado)
	}
}
