package main

import "fmt"

func main() {
	var num int

	println("escreva um numero")
	fmt.Scan(&num)

	fmt.Printf("divisores de %d: ", num)
	for i := 1; i <= num; i++ {
		if num%i == 0 {
			fmt.Printf("%d ", i)
		}
	}

}
